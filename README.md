# AI Assistant Pro Full

Playful theme + Monaco editor + download conversation

Run locally:

1. npm install
2. Add .env.local with OPENAI_API_KEY
3. npm run dev

Deploy to Vercel: push to GitHub and set OPENAI_API_KEY in Project Environment Variables.
